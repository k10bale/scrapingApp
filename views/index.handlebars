
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#"><img src="/assets/images/eagles-logo.png" width="30" height="30" class="d-inline-block align-top" alt="">Philidelphia Eagles News Scraper</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <a class="nav-item nav-link active" href="/">Home <span class="sr-only">(current)</span></a>
            <a class="nav-item nav-link" href="/saved">Saved Articles</a>
          </div>
        </div>
        <form class="form-inline">
            <button class="btn btn-outline-success scrape-new" type="button">Scrape Articles</button>
            <button class="btn btn-outline-success clear" type="button">Clear Articles</button>
          </form>
      </nav>
  

  <div class="jumbotron jumbotron-fluid text-center">
    <div class="container">
  <h1 class="display-4">Eagles News</h1>
  <p class="lead">Get the latest from NJ.com</p>
</div>
</div>

<div class="container article-container">
  <h3>Articles</h3>
  {{#each articles}}
    <div class="card bg-light mb-3">
      <div class="card-body">
        <h5 class="card-title">{{title}}</h5>
        <a href="{{link}}" class="card-link" target="_blank">View article on NJ.com</a>
        <p class="card-text">{{intro}}...</p>
        <a data-articleId="{{_id}}" class="btn btn-primary save-btn">Save article</a>
      </div>
    </div>
  {{/each}}
</div>